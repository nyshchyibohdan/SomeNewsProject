export interface UploadPicDataDTO {
    profilePic: string;
}

export interface UpdateBioDTO {
    bio: string;
}

export interface ChangePasswordDTO {
    oldPassword: string;
    newPassword: string;
}
